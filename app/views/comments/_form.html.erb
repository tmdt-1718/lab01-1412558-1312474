<%= form_for ([@post, @post.comments.build]) do |f| %>
<div class="form-group">
  <%= f.text_area :body, class: "form-control", placeholder: "Your body", required: true, rows: 5%>
</div>
<div class="form-group">
  <%= f.submit "Comment", class: "btn btn-primary" %>
</div>
<% end %>
